# Board is a 8x8 matrix, each cell is either empty or stores a piece.
# Base class "Piece" with attributes: colour, position
# Subclass includes: Pawn, Rook, Knight, Bishop, Queen, KIng
# Each subclass implements valid_moves(board)
